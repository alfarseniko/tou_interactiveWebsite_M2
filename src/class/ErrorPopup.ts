export class ErrorPopup {
  constructor(errorMessage: string) {
    // give the string to a function which creates the error
  }
}

/** The class receives an error message and then displays it as a custom popup error */
